<%- include("partials/header") %>

<main class="content">
  <h1><%= title %></h1>
  <p>
    This course hub keeps class information separated by task so each page stays focused.
  </p>
  <p>
    Use the catalog to compare sections, the faculty directory to find instructor details,
    and the request page to confirm the current page context.
  </p>
</main>

<%- include("partials/footer") %>
